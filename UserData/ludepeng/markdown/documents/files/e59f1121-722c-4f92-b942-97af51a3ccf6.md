## 道具、资源相关
- aps_resource_item
    ```Lua
    -- Icon
    DataCenter.ResourceItemDataManager:GetIconPath(itemId)
    ```


local uuid = 1435569141045005590
local arrowData = {
    arrowType = HeroDetailGuideArrowType.Rank,
    heroUid = uuid,
}
UIManager:GetInstance():OpenWindow(UIWindowNames.UIHeroDetailPanel, { anim = false }, uuid, { uuid }, nil, arrowData);