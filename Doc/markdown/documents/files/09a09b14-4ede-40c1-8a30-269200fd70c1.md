可以。建议按“GM 开启 → Online 关闭 → GM 回滚”三条主路径测试，每条路径都从卸载重装 15325 开始，避免包外文件和 ClientSwitch 缓存互相污染。

我已经确认本机 Android SDK 的 ADB 在：

```text
C:\Users\Administrator\AppData\Local\Android\Sdk\platform-tools\adb.exe
```

设备接入后，我可以直接抓取和分析 logcat，你不用手工复制。

## 测试基线

| 项目 | 旧包 15325 | 新版本 17552 |
|---|---|---|
| 二进制逻辑 | 无 | 有 |
| GameRes | 247，仅文本 | 314，文本+二进制 |
| DllRes | 230，仅文本 | 292，文本+二进制 |
| ClientSwitch 53 | 不使用，但旧 `Parse()` 可能仍会缓存该位 | 正式读取 |
| GM 开关 | — | 1 |
| Online 开关 | — | 0 |

17552 的本地 version 预期内容：

```text
gameres.version
314,14928254,2734187270,3043287773

dllres.version
292,31027,2273094679,3274857497
```

其中最后一列是 binCRC。

如果使用 Debug 包指定 Build ID 17552，下载 URL 应该是：

```text
gameres_small_v17552
gameres_small_v17552.man
dllres_small_v17552
dllres_small_v17552.man
```

Manifest 内部版本仍然是 GameRes 314、DllRes 292。

如果是不指定 Build ID 的普通自动更新，URL 才使用：

```text
gameres_small_v314(.man)
dllres_small_v292(.man)
```

## 路径一：GM 用户首次升级

服务端准备：

```text
GM ClientSwitch[53] = 1
Online ClientSwitch[53] = 0
```

同时确认 CDN 上 17552 的文本和 `.man` 都已上传。

### GM-0：安装旧包后的干净状态

操作：

1. 卸载游戏。
2. 安装 15325。
3. 暂时不要启动。
4. USB 连接设备。
5. 告诉我“15325 已安装，准备开始 GM 测试”。

我会记录：

- 实际包名、versionCode。
- 包外 `AssetBundles` 是否为空。
- `gameres.version/dllres.version` 是否存在。
- ClientSwitch 53 是否有缓存。
- 清空 logcat，建立基线。

预期：

```text
包内：GameRes 247 TEXT、DllRes 230 TEXT
包外：无 Manifest
ClientSwitch 53：无缓存
```

### GM-1：15325 第一次更新到 17552

操作：

1. 启动游戏。
2. 开启 Dev 热更并选择 Build ID 17552。
3. 进入账号页面。
4. 点击进入游戏。
5. 每次自动重启或回到 Build ID 页面时先告诉我，不要立刻继续。

预期：

- 15325 没有二进制逻辑，所以第一次只能下载文本 Manifest。
- 下载内容版本为 GameRes 314、DllRes 292。
- 下载 DllRes 292 后获得最新二进制代码。
- 因 DLL 更新触发重启。
- 旧版 `ClientSwitch.Parse()` 即使没有常量 53，也可能把服务端第 54 项保存成 `CLIENT_SWITCH_CACHE_ON_53=1`；我们会实际检查。

阶段结束时预期状态：

```text
包内：247/230 TEXT
包外：314/292 TEXT
包外 .man：暂时可能不存在
二进制代码：已经更新，但等待重启生效
ClientSwitch 53 缓存：预期为1
```

### GM-2：新代码首次启动，补下载同版本二进制

自动重启后，不要立即连续点击，先让我抓一次日志。

预期启动顺序：

```text
读取缓存开关53 = 1
→ UseBinaryManifest = true
→ 检查包外 version
→ 由于旧逻辑保存的 version 可能还没有 binCRC
→ 启动仍加载包外文本 314/292
→ CheckResVersion 返回带 binCRC 的新 version
→ 在线开关53 = 1
→ 发现当前内存是 TEXT，但目标格式是 BIN
→ 下载同版本 .man
→ 解析二进制
→ 持久化 gameres.man/dllres.man 和四列 version
```

日志重点：

```text
binary switch, source:cache, switchOn:True
binary switch, source:server, switchOn:True
download manifest:gameres, hasBin:True, useBinary:True
download manifest:dllres, hasBin:True, useBinary:True
parse-binary done
同版本格式持久化
持久化 Manifest
```

因为同版本格式变化采用“不刷新当前内存”的策略，本次进入游戏后 UI 仍可能显示 `_T`，这是允许的；二进制文件会在下次冷启动正式使用。

阶段结束时必须确认：

```text
包外 gameres.man 存在
包外 dllres.man 存在
gameres.version 为四列，版本314
dllres.version 为四列，版本292
```

### GM-3：杀进程后冷启动

操作：

1. 正常进入游戏后杀掉进程。
2. 不卸载、不清数据。
3. 清空 logcat。
4. 重新启动并进入游戏。
5. 告诉我每个页面状态。

预期：

```text
source:cache, switchOn:True
包外 version hasBin=True
sync use gameres source=download format=binary version=314
sync use dllres source=download format=binary version=292
parse-binary done
UI 资源版本后缀为 _B
```

本次不应该：

- 再次下载 `gameres_small_v17552.man`。
- 再次下载 `dllres_small_v17552.man`。
- 回退文本。
- 出现黑屏或 `LoadQueue` 空引用。

### GM-4：第二次稳定性重启

再杀进程重启一次。

通过标准：

- 仍然直接加载包外二进制。
- 每个 Manifest 启动阶段只解析一次。
- 不重复下载。
- UI 继续显示 `_B`。
- GameRes=314，DllRes=292。
- 没有 fallback、CRC mismatch、missing、parse failed。

## 路径二：Online 用户保持文本

必须重新卸载并安装 15325，不能复用 GM 测试数据。

服务端：

```text
Online ClientSwitch[53] = 0
```

### ONLINE-0：干净安装

操作与 GM-0 相同。

预期：

```text
包内：247/230 TEXT
包外：空
ClientSwitch 53：无缓存
```

### ONLINE-1：更新到 17552

选择相同 Build ID 17552，完成首次更新和 DLL 重启。

预期：

- 旧代码仍下载文本 314/292。
- ClientSwitch 53 缓存为 0。
- 新代码启动后 `UseBinaryManifest=false`。
- 即使 CheckResVersion 返回 binCRC，也选择文本。

关键日志：

```text
binary switch, source:cache, switchOn:False
binary switch, source:server, switchOn:False
download manifest:gameres, hasBin:True, useBinary:False
download manifest:dllres, hasBin:True, useBinary:False
parse-text done
```

允许因为版本已经相同而直接跳过 Manifest 下载。

### ONLINE-2：连续两次冷启动

通过标准：

- 始终只有 `parse-text`。
- 不出现 `parse-binary`。
- 不下载 `.man`。
- 包外没有因客户端主动下载产生 `.man`。
- UI 显示 `_T`。
- GameRes=314，DllRes=292。
- 不重复下载文本 Manifest。

## 路径三：GM 紧急回滚

这条路径复用 GM-4 已经稳定使用二进制的设备。

操作：

1. 服务端把 GM 的开关 53 改为 0。
2. 保留 CDN 上全部文本和 `.man`。
3. 杀进程重新启动。

第一次启动预期：

```text
缓存仍为1
→ 启动早期仍加载包外 BIN
→ CheckResVersion 获取在线开关0
→ UseBinaryManifest 改为false
→ 发现当前内存 BIN、目标 TEXT
→ 下载/持久化同版本文本
```

因为 Manifest 冷启动早于 CheckResVersion，远端关闭存在一次启动延迟，这是当前机制的正常行为。

再次杀进程重启，预期：

```text
缓存开关53 = 0
→ 直接加载包外文本
→ UI 显示 _T
→ 不再解析二进制
```

通过标准：

- 关闭开关后不会黑屏。
- 下一次冷启动稳定回到文本。
- 不要求删除 `.man`。
- 不需要卸载资源引用。
- 不发生每次登录反复切换或重复下载。

## 可选路径：验证本地二进制缺失回退

主流程通过后，可以做一次故障注入：

1. 保留四列 version。
2. 将包外 `gameres.man`改名为备份文件，不直接删除。
3. 保留包外文本 `gameres`。
4. 开关 53 保持为 1。
5. 冷启动。

预期：

```text
包外 BIN missing
→ 回退包外 TEXT
→ 正常进入游戏
→ CheckResVersion 后重新下载 BIN
```

执行这个故障注入前，我会再次确认设备中的准确文件路径，并采用可恢复的改名操作。

## 每个阶段我会抓取的数据

日志过滤范围：

```text
MANIFEST_TRACE
CheckResVersion
ClientSwitch
DownloadManifest
DownloadUpdateState
OverrideManifest
Loading SetState
FileNotFoundException
NullReferenceException
LoadQueue
```

同时检查设备文件：

```text
AssetBundles/gameres
AssetBundles/gameres.man
AssetBundles/gameres.version
AssetBundles/dllres
AssetBundles/dllres.man
AssetBundles/dllres.version
```

以及：

```text
CLIENT_SWITCH_CACHE_ON_53
APK versionCode
当前进程 PID
```

## 上线验收结论

第一版允许上线需要同时满足：

- GM 路径最终稳定为 `_B`。
- Online 路径始终为 `_T`。
- GM 第二、第三次启动不重复下载 `.man`。
- Online 不下载 `.man`。
- 开关关闭后下一次冷启动可以回到文本。
- `.man` 缺失或损坏能够回退文本。
- 没有新增黑屏、崩溃和 LoadQueue 异常。
- GameRes 314、DllRes 292 始终匹配，不出现混合旧版本。

还有一个上线前安全点：当前代码在 `client_config` 字段完全缺失时，可能沿用本进程此前解析过的开关值。GM/Online Jenkins 如果保证每次响应都带完整 `client_config`，不影响这次测试；正式上线前建议额外验证“字段缺失/空字符串”场景是否需要强制关闭。

设备准备好后，先保持 15325 未启动并告诉我：“设备已连接，开始 GM-0”。我会从设备识别和干净状态检查开始。